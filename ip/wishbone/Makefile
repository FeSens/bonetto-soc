.PHONY: formal sim clean

# The wishbone IP is currently header-only; nothing to formal-verify in iter 1.
# Arbiter + xbar in iteration 2 get their own checks.
formal:
	@echo "[wishbone] no synthesisable RTL yet (iter 1 is header-only)"

sim:
	@echo "[wishbone] no sim yet"

clean:
	@true
